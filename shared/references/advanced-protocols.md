# Advanced Protocol Reference Index

This compatibility index replaces the former all-in-one reference. Load only the focused file needed for the task:

- `shared/references/signatures.md` for EIP-712, permit, contract signatures, nonces, and replay protection.
- `shared/references/oracle-safety.md` for price-source validation, normalization, liveness, and circuit breakers.
- `shared/references/cross-chain-l2.md` for messaging, finality, bridge trust, and L2 behavior.
- `shared/references/modern-evm.md` for compiler targets, transient storage, namespaced storage, and delegation.
- `shared/references/protocol-operations.md` for privileged operations, monitoring, and incident response.
- `shared/references/mev-market-mechanics.md` for slippage, ordering, auctions, liquidations, and solver risk.
- `shared/references/reproducible-builds.md` for dependency locks, compiler settings, bytecode checks, and verification.

Existing links may continue to point here. New work should reference the focused file directly and dry-run any live operation before execution.
