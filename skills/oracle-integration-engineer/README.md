# Oracle Integration Engineer

Load `SKILL.md` for oracle consumer design and review. The local references distinguish provider update models and common failure paths; templates provide a mockable normalized interface.

Oracle adapters remain protocol-specific and require target-chain configuration review.
