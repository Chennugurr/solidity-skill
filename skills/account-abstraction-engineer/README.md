# Account Abstraction Engineer

Load `SKILL.md` for ERC-4337 accounts, factories, paymasters, modular authorization, and EIP-7702 delegation. Load the local references only for the account mechanism being used.

The included paymaster is deliberately test-only and must not be deployed with real funds.
